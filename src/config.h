/* Only define one, or neither! */
#define dbt_AUX_IS_INPUT (true)
// #define dbt_AUX_IS_MOTOR (TRUE)

// Defaults to FALSE if commented out
#define dbt_MOTOR_0 (true)
#define dbt_MOTOR_1 (true)

#define DISPLAY_BACKLIGHT_CONTROL (true)
#define DISPLAY_TOUCH_SCREEN (true)

#define CUSTOM_MACHINE_NAME "dbt controller"


#include "../inc/pin_mappings.h"
